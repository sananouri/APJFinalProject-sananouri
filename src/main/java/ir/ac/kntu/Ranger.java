package ir.ac.kntu;

public class Ranger extends Soldier {

    public Ranger(int energy, int lives,
                  int maxSpeed, int damageRate, int range, SoldierType type) {
        super(energy, lives, maxSpeed, damageRate, range, type);
    }
}